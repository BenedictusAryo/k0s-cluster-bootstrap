{{- if .Values.cilium.gatewayAPI.enabled }}
---
<<<<<<< HEAD
=======
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: argocd-route
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  parentRefs:
  - name: cloudflare-gateway
    namespace: gateway-system
  hostnames:
  - "{{ .Values.argocd.ingress.hostname }}"
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: argocd-server
      port: 80  # ArgoCD server is running with --insecure flag, so use HTTP port
      namespace: argocd
>>>>>>> 72068435a2d37b2618861df27c5f46d795ee9e15
{{- end }}
